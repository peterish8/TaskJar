interface:
  display_name: "TaskJar"
  short_description: "Repo-specific patterns and workflows for TaskJar"
  default_prompt: "Use the TaskJar repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true